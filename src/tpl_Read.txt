create procedure [$schema].[${table}_Read]
	@id integer
as
begin

	select
#foreach ($c in $columns)
		$t.[$c.getName()]#if (!$foreach.isLast()),#end

#end		
	from [$schema].[$table] $t
	where id = @id
	
end
go
